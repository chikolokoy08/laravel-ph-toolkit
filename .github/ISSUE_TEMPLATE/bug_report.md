---
name: Bug report
about: Something does not work as documented
labels: bug
---

**What happened**

**What you expected**

**Reproduction**

```php
// The smallest example that shows it, with the input value.
```

**Versions**

chikolokoy08/laravel-ph-toolkit:
Laravel:
PHP:

`php artisan about --only=ph_toolkit` reports the first two.

**If this is about address data**

The PSGC code of the place, and what the PSA publication says.
`Address::psgcVersion()` tells you which release you have.

**If this is about the address endpoints or the selector**

Your `config/ph-toolkit.php`, or the `PH_TOOLKIT_` values in your `.env`.
